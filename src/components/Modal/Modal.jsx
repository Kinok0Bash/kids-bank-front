import './Modal.scss';
import ModalWrapper from "./components/ModalWrapper.jsx";
const Modal = ({...props}) => {
    const {
        type
    } = props;
    return (
        <ModalWrapper>
            {'хуй'}
        </ModalWrapper>
    )
}

export default Modal;